import { Theme } from "./theme.interface";
import ThemeContextProvider, { ThemeContext } from "./ThemeContext";
import { defaultLightTheme } from "./themes";

export type { Theme };
export { ThemeContextProvider, ThemeContext, defaultLightTheme };
